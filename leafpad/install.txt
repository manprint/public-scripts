curl -o leafpad.deb -L https://seafile.devopsita85service.ga/f/33ec9fec0d1f43ae81da/?dl=1

sudo apt install -y ./leafpad.deb

rm -f ./leafpad.deb


------------------------

from github
-----------

curl -o leafpad -L https://github.com/manprint/public-scripts/raw/master/leafpad/leafpad_0.8.18.1-5_amd64.deb

sudo apt install -y ./leafpad.deb

rm -f ./leafpad.deb
